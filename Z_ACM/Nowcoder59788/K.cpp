#include <iostream>
#include <vector>
#include <stdlib.h>
#include <math.h>
#include <string>
#include <string.h>
#include <algorithm>

#define ull unsigned long long
#define ld long double
#define maxn 100010
#define modq 1000000007
#define ll long long

using namespace std;

void solve()
{
    int N;
    cin >> N;
    if(N == 2) cout << "26 650 0\n";
    ll int ANSm = 26;
    for()
    return;
}

int main()
{
    int T = 1;
    std::ios::sync_with_stdio(false);
    cin >> T;
    for(int i = 0;i < T;i ++)
    {
        solve();
    }
    system("pause");
    return 0;
}