#include <bits/stdc++.h>

const int TEST_TYPE = 1;// The type of cases you want to test

void solve()
{

// your code

};

signed main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    std::cout.tie(0);

    int T;
    std::cin >> T;
    
    if(T == TEST_TYPE) solve();
    else std::cout << "TESTLIB_FORCES_AC\n";

}