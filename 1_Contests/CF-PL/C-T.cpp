#include <iostream>
#include <vector>

using namespace std;


int main()
{
    for(;;)
    {
        int n;
        cin >> n;
        int k;
        cin >> k;
        vector<int> arr(n+1);
        for(int i = 0;i < n;i ++)
        {
            cin >> arr[i];
        }
        
    }
    system("pause");
    return 0;
}