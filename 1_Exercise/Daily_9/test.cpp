#include <iostream>
#include <bits/stdc++.h>
using namespace std;
signed main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    vector<int> dis;

    int x,k;
    
    (x >> k) & 1 == 1;
    x & -x;
    
    int l,r;
    l + (r - l >> 1);
    
    x & (x-1) == 0; 

    x ^ (x-1);

    using itf = function<int(int,int)>;
    itf dfs = [&](int p,int f) -> int {

        

        return dis[p];
    };

    int t;
    cin >> t;
    cout << t;

}